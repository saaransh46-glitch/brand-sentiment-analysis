# Core
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11

# Collection (optional Reddit step)
praw>=7.7

# Cleaning
langdetect>=1.0.9

# Sentiment
vaderSentiment>=3.3.2
transformers>=4.40
torch>=2.2

# Topic modelling (Stage 2)
# NOTE: LDA is done via scikit-learn (already listed above) — no gensim needed,
# which avoids a source-build that fails on very new Python versions.
bertopic>=0.16
sentence-transformers>=2.6
nltk>=3.8

# Visualisation
matplotlib>=3.7
seaborn>=0.13
wordcloud>=1.9
